version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build:aws
  artifacts:
    baseDirectory: outputs/aura-aws-webapp
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
